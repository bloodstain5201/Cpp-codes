#include<iostream.h>
#include<conio.h>
#include<math.h>
    void main()
    {
       clrscr();
       int n;
       float x,sum=0,pro=1,pw=0,result;
       cout<<"\nEnter x and n";
       cin>>x>>n;
       for(int i=0;i<n;i++)
       {
	 pro*=(i+1);
	 pw=i+2;
	 sum+=(pow(-1,i)*pow(x,pw))/pro;
       }
       result=x+sum;
       cout<<"\n"<<result;
       getch();
    }