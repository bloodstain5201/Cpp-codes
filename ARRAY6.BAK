#include<iostream.h>
#include<conio.h>
   void main()
   {
      int n,a[20],x=0;
      cout<<"\nEnter the no of components in the array";
      cin>>n;
      for(int b=0;b<n;b++)
      {
	cout<<b+1<<"number";
	cin>>a[b];
      }
      for(int i=0,j=n/2;i<n/2;i++,j++)
      {
	  x=a[i];
	  a[i]=a[j];
	  a[j]=x;
      }
      cout<<"\n";
      for(b=0;b<n;b++)
      cout<<" "<<a[b];
      getch();
   }